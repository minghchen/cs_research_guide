# 浙大服务器配Pytorch环境指南

[TOC]

假设要在浙大实验室服务器上配置环境：`python=3.7.9 pytorch=1.6.0 torchvision=0.7.0 cudatoolkit=10.1`。

那第一步当然是要连上服务器。

## SSH连接服务器

联系服务器管理员（目前为康天楠）在目标服务器上开通用户。

以我的用户名chenminghao和10.76.1.48服务器为例。连接48服务器：

```
ssh chenminghao@10.76.1.48
```

默认端口为22，但目前有些服务器需要用8899端口，则需在命令中加上`-p 8899`。

目前无法使用密码登录，统一使用私钥和公钥登录。

一个完整又方便的SSH教程文档：[SSH教程](https://wangdoc.com/ssh/client.html)

简单来说，就是要生成自己电脑上的私钥和公钥，然后放到服务器上，就可以无需密码登录服务器：

以下命令生成私钥和公钥，私钥位于`~/.ssh/id_rsa`，公钥位于`~/.ssh/id_rsa.pub`

```
ssh-keygen -t rsa
```

~~可用ssh-copy-id chenminghao@10.76.1.48`将公钥上传到服务器的`~/.ssh/authorized_keys`文件:~~ （目前不可用）

```
cat ~/.ssh/id_rsa.pub
```

将输出的公钥发给管理员，他会放入服务器你的目录下的`~/.ssh/authorized_keys`中。

用户的初始密码为123456。可使用`passwd`命令更改密码。


## 检查CUDA环境

使用`nvidia-smi` 查看服务器的驱动版本，以及驱动对应的CUDA版本。由下图可见，48服务器现在驱动版本是470.82.00，它所支持的最高CUDA版本是11.4。如果这个CUDA version低于你想要的cuda版本，只能联系管理员升级驱动 [NVIDIA® GPU 驱动程序](https://www.nvidia.com/drivers)。

<img src="pictures/截屏2024-09-21 12.40.37.png" alt="截屏2024-09-21 12.40.37" style="zoom:33%;" />

接着查看/usr/local下已有的cuda库，发现已经有cuda-10.1和cuda-11.4，均高于或等于想要装的cudatoolkit=10.1，那么两者都可以用。

<img src="pictures/截屏2024-09-21 12.41.13.png" alt="截屏2024-09-21 12.41.13" style="zoom: 50%;" />

最后查看cuda库是否包含cudnn包。发现cuda-11.4并没有cudnn，而cuda-10.1有装cudnn，因此选择cuda-10.1进行下一步。

<img src="pictures/截屏2024-09-21 12.43.52.png" alt="截屏2024-09-21 12.43.52" style="zoom:50%;" />



### 设置cuda路径

因为cuda-10.1有装cudnn，而cuda-11.4并没有cudnn。现在系统默认的cuda是cuda-11.4，那么需要将cuda-10.1设置为我们要用的cuda库。

`vi ~/.bashrc`加入这几行：

```
# CUDA
export CUDA_HOME="/usr/local/cuda-10.1"
export PATH="/usr/local/cuda-10.1/bin:$PATH"
export LD_LIBRARY_PATH="/usr/local/cuda-10.1/lib64:$LD_LIBRARY_PATH"
```

之后重启命令行，就可以将默认的cuda路径变为cuda-10.1库



### 无root权限在自己目录下安装cuda

如果想装cudatoolkit=10.2，或者在别的服务器上没有足够高版本的cuda库或者不包含cudnn包怎么办。解决方法是求人不如求己，在自己的目录下装一个cuda库。

一种讨巧的方法是，如果当前服务器没有想要的cuda库，可以看看其他服务器有没有想要的cuda库。然后直接复制其他服务器的cuda库到自己的目录下，并且将cuda路径设置为这个库。

如将48服务器上的cuda-10.1库复制到其他服务器上：

```
scp -r chenminghao@10.76.1.48:/usr/local/cuda-10.1 /home/chenminghao/
```

如果其他服务器都没有想要的库，那就需要下载安装cuda库

参考攻略 [非root用户安装cuda与cudnn](https://zhuanlan.zhihu.com/p/198161777)

#### 下载cuda和cudnn

下载网址 https://developer.nvidia.com/cuda-toolkit-archive

以下载cuda的10.2为例：

<img src="pictures/截屏2024-09-21 12.38.17.png" alt="截屏2024-09-21 12.38.17" style="zoom: 33%;" />

此时你可以选择在服务器上输入

```text
wget https://developer.download.nvidia.com/compute/cuda/10.2/Prod/local_installers/cuda_10.2.89_440.33.01_linux.run
```

在服务器上进行下载，也可以选择在浏览器中输入wget后面的网址，然后在本地进行下载再上传至服务器。

通过[网址](https://link.zhihu.com/?target=https%3A//developer.nvidia.com/rdp/cudnn-download)下载CUDNN，这个下载需要注册账号（简单几步注册登录即可），登录以后，如下图选择合适的CUDA版本对应的CUDNN并选择CUDNN Library for Linux，开始下载，下载好以后将文件后缀名改为.tgz后上传至服务器。

#### 安装CUDA和cudnn

无需root权限安装cuda

```
# 给cuda可执行权限
chmod +x cuda_10.2.89_440.33.01_linux.run

# 运行run文件
sh cuda_10.2.89_440.33.01_linux.run
```

安装时将默认路径和Library install path修改至个人目录下

安装cudnn

 ```
# 解压cuDNN，输入以下命令进行解压
tar -zxvf cudnn-10.2-linux-x64-v8.0.0.39.tgz
 
# 此时当前目录下会出现一个./cuda的文件夹。复制文件到CUDA安装目录
cp ./cuda/include/cudnn.h ~/cuda-10.2/include/
cp ./cuda/lib64/libcudnn* ~/cuda-10.2/lib64/ 

# 修改权限，cudnn安装完成
chmod a+r ~/cuda-10.2/include/cudnn.h ~/cuda-10.2/lib64/libcudnn*
 ```

#### 修改cuda路径

最后vi ~/.bashrc修改cuda路径的环境变量

```text
# CUDA
export CUDA_HOME=$CUDA_HOME:/home/chenminghao/cuda-10.2
export PATH=$PATH:/home/chenminghao/cuda-10.2/bin
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/home/chenminghao/cuda-10.2/lib64
```



## 使用conda下载pytorch

### 设置conda和pip的镜像

设置conda镜像（北外）：vi ~/.condarc

```
channels:
  - defaults
show_channel_urls: true
ssl_verify: false
channel_alias: https://mirrors.bfsu.edu.cn/anaconda
default_channels:
  - https://mirrors.bfsu.edu.cn/anaconda/pkgs/main
  - https://mirrors.bfsu.edu.cn/anaconda/pkgs/free
  - https://mirrors.bfsu.edu.cn/anaconda/pkgs/r
  - https://mirrors.bfsu.edu.cn/anaconda/pkgs/pro
  - https://mirrors.bfsu.edu.cn/anaconda/pkgs/msys2
  - https://mirrors.bfsu.edu.cn/anaconda/cloud/conda-forge
  - https://mirrors.bfsu.edu.cn/anaconda/cloud/pytorch
custom_channels:
  conda-forge: https://mirrors.bfsu.edu.cn/anaconda/cloud
  msys2: https://mirrors.bfsu.edu.cn/anaconda/cloud
  bioconda: https://mirrors.bfsu.edu.cn/anaconda/cloud
  menpo: https://mirrors.bfsu.edu.cn/anaconda/cloud
  pytorch: https://mirrors.bfsu.edu.cn/anaconda/cloud
  simpleitk: https://mirrors.bfsu.edu.cn/anaconda/cloud
```

设置pip镜像（阿里云）：vi ~/.pip/pip.conf

```
[global]
trusted-host = mirrors.aliyun.com
index-url = http://mirrors.aliyun.com/pypi/simple
```

### 创建python3.7环境和安装GPU版的pytorch

```
# create conda env and install packages
conda create -y --name py37 python=3.7.9

conda activate py37
conda install -y pytorch==1.6.0 torchvision==0.7.0 cudatoolkit=10.1 -c pytorch
```

注意cudatoolkit版本只需要低于或等于服务器上cuda的版本就行，不一定要完全等于。

测试是否安装成功，如果输出为True则成功：

```
python -c "import torch;print(torch.cuda.is_available())"
```



## 使用vscode编程

### 简易使用git和github

设置git代理和邮箱，由vi /etc/proxychains.conf可知代理端口

```
git config --global http.https://github.com.proxy socks5://10.76.7.167:7070
git config --global https.https://github.com.proxy socks5://10.76.7.167:7070

git config --global user.name "Minghao Chen"
git config --global user.email minghaochen01@gmail.com
```

使用vscode编程时可以很方便地进行git init，git add，git commit，git push等操作，来实现与github代码的同步。

<img src="pictures/截屏2024-09-21 12.35.05.png" alt="截屏2024-09-21 12.35.05" style="zoom: 33%;" />

使用git push到github前先将机器的ssh key上传到github中

<img src="pictures/截屏2022-05-07 下午7.27.25.png" alt="截屏2022-05-07 下午7.27.25" style="zoom:50%;" />

